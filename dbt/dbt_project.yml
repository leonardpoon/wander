# US-43, US-44, US-45, US-46 — dbt project configuration

name: wander
version: '1.0.0'
profile: wander

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  wander:
    staging:
      +materialized: view
      +schema: staging

    intermediate:
      +materialized: view
      +schema: intermediate

    mart:
      +materialized: table
      +schema: mart